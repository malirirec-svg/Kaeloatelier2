import {Link} from 'react-router-dom'; import Icon from './Icon'; import {useShop} from '../context/ShopContext';
export default function ProductCard({product}){const {wishlist,toggleWishlist,addToCart}=useShop(); const wished=wishlist.includes(product.id); return <article className="group"><div className="relative bg-cream overflow-hidden aspect-[4/5]"><Link to={`/product/${product.id}`}><img src={product.image} alt={product.name} className="w-full h-full object-cover mix-blend-multiply transition duration-500 group-hover:scale-[1.03]"/></Link><button onClick={()=>toggleWishlist(product.id)} className={`absolute right-3 top-3 w-9 h-9 rounded-full bg-white flex items-center justify-center shadow-sm ${wished?'text-leaf':'text-slate-700'}`}><Icon name="heart" size={18}/></button>{product.stock<5&&<span className="absolute left-3 top-3 bg-white/95 text-[10px] uppercase tracking-wider px-2 py-1">Low stock</span>}</div><div className="pt-3"><Link to={`/product/${product.id}`}><h3 className="font-medium">{product.name}</h3></Link><div className="mt-1 flex items-center gap-2"><span className="text-sm">UGX {product.price.toLocaleString()}</span>{product.compareAt&&<span className="text-xs text-slate-400 line-through">UGX {product.compareAt.toLocaleString()}</span>}</div><button onClick={()=>addToCart(product,product.sizes?.[0])} className="mt-3 w-full border border-navy text-navy py-2 text-xs uppercase tracking-wider hover:bg-navy hover:text-white transition">Add to cart</button></div></article>}
