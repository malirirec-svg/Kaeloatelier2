import {Link} from 'react-router-dom'; import ProductCard from '../components/ProductCard'; import {products} from '../data/products'; import {useShop} from '../context/ShopContext';
export default function Wishlist(){const {wishlist}=useShop(); const items=products.filter(p=>wishlist.includes(p.id)); return <main className="container-kaelo py-10 md:py-16"><p className="text-[11px] uppercase tracking-[.25em] text-leaf">Saved pieces</p><h1 className="serif text-4xl md:text-5xl text-navy mt-2">Wishlist</h1>{items.length===0?<div className="py-20 text-center"><p className="text-slate-500">Save pieces you love and they’ll appear here.</p><Link to="/products" className="inline-block mt-6 bg-navy text-white px-6 py-3 text-xs uppercase tracking-wider">Explore the collection</Link></div>:<div className="grid grid-cols-2 md:grid-cols-4 gap-5 md:gap-7 mt-10">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div>}</main>}
